name: ath615v2
description: Athlete 615 gym app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.4
  go_router: ^17.2.2
  flutter_dotenv: ^6.0.1
  app_links: ^7.0.0
  image_picker: ^1.2.2
  firebase_messaging: ^16.2.0
  firebase_core: ^4.7.0
  shared_preferences: ^2.5.5
  flutter_localizations:
    sdk: flutter
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
