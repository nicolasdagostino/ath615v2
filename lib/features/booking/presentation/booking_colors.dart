import 'package:flutter/material.dart';

/// Booking's local interactive color. It intentionally does not affect the
/// application-wide accent while this visual direction is under review.
abstract final class BookingColors {
  static const primary = Color(0xFF159ED1);
}
